# This Django-based project is designed to help manage and showcase collection of gadgets

## Run the projet locally using docker compose
```bash
docker compose -f docker-compose.yml up -d --build
```

```
http://localhost:8080
```
